export function ToPrimitive(value: any): any {
  // TODO: Realize
  return value;
}

export function Type(value: any): string {
  return typeof value;
}

export function ToNumber(value: any): number {
  return Number(value);
}
